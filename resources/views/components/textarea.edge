<div class="form-group form-group-default {{ required ? 'required' : null }}">
  <label for="{{ name }}">{{ text }}</label>
  <textarea placeholder="{{ placeholder }}" class="form-control" name="{{ name }}" style="height: 50px;">
@if(value)
{{ value }}
@endif
</textarea>
@if(error)
<span>
  {{ error }}
</span>
@endif
</div>
